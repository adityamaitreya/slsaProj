# Project Explanation — What We Have Built So Far

> This file is updated after every completed stage.
> It explains what was built, why it was built, and how it works — in plain English.

---

## The Big Idea

We are building a **software supply chain security system**.

The question it answers is:

> **"Can I trust this software? Can I prove where it came from, who built it, and that nobody touched it?"**

Think of it like a food product. When you buy a jar of tomato sauce, the label tells you:
- What's inside (ingredients)
- Where it was made (factory)
- When it was made (date)
- That nobody tampered with it (sealed cap)

We are building the same thing — but for software.

---

## How the System Works (Simple View)

```
You write code
      ↓
Push to GitHub
      ↓
GitHub builds it automatically (trusted factory)
      ↓
Docker image created with a unique fingerprint (SHA-256)
      ↓
Ingredient list generated (SBOM)
      ↓
Build receipt generated (SLSA Provenance) ← coming next
      ↓
Everything signed cryptographically (Cosign) ← coming next
      ↓
Stored in database (PostgreSQL) or blockchain (IPFS)
      ↓
Anyone can verify: VALID / TAMPERED / INVALID
```

We have completed the **top 4 steps** so far.

---

## Stages Completed

---

### Stage A1 — Sample Application

**What it is:**
A simple Python web API built with Flask. This is the "software artifact" we are going to track through the supply chain.

**Why we built it:**
Every supply chain needs a product to track. We chose a simple Flask app so we can focus on the supply chain mechanics rather than complex application logic.

**What it does:**

| Endpoint | What it returns |
|---|---|
| `GET /` | App name, version, status |
| `GET /health` | Is the app running? Uptime, Python version |
| `GET /api/artifact-info` | Metadata — version, source repo, builder |
| `POST /api/echo` | Send JSON, get it back with timestamp |

**Files created:**

| File | Purpose |
|---|---|
| `app/src/app.py` | The Flask application (143 lines) |
| `app/tests/test_app.py` | 7 automated tests |
| `app/requirements.txt` | Python dependencies (Flask, Gunicorn, Werkzeug) |
| `app/.env.example` | Configuration template |
| `app/README.md` | Application documentation |
| `app/run_dev.sh` | Script to run locally during development |
| `app/validate_structure.py` | Script to confirm Stage A1 is complete |

**Key concept:**
The `/api/artifact-info` endpoint is important. It returns the app's metadata (name, version, build timestamp, source repo). Later in the project, SLSA provenance will verify that this metadata is authentic and hasn't been tampered with.

---

### Stage A2 — Dockerization

**What it is:**
Packaging the Flask app into a Docker container so it runs the same way everywhere.

**Why we built it:**
Docker produces a **SHA-256 digest** — a unique fingerprint for the built image. This fingerprint is the foundation of our entire supply chain. If anyone changes even one byte in the code, the fingerprint changes. That is our tamper detection.

**What is a SHA-256 digest?**

```
sha256:5fbc20ebcfdd...
```

This is a 64-character hash generated by running a mathematical function over the entire contents of the Docker image. It is:
- **Unique** — no two different images produce the same hash
- **Deterministic** — same input always produces same hash
- **One-way** — you cannot reverse it to get the original image
- **Tamper-evident** — any change produces a completely different hash

**Multi-stage Dockerfile explained:**

```dockerfile
# STAGE 1: Builder
# Use a full Python image to install dependencies
FROM python:3.11-slim as builder
RUN pip install -r requirements.txt
COPY src/ ./src/

# STAGE 2: Runtime
# Use a minimal image — only copy what is needed to RUN the app
FROM python:3.11-slim as runtime
COPY --from=builder ...
USER appuser          # non-root user for security
CMD ["gunicorn", ...]  # production server
```

Why two stages?
- Stage 1 installs build tools and dependencies
- Stage 2 copies only the final result — no build tools, no clutter
- Result: smaller image (207MB), smaller attack surface

**Files created:**

| File | Purpose |
|---|---|
| `app/Dockerfile` | Production multi-stage build (100 lines) |
| `app/Dockerfile.dev` | Development build (hot reload) |
| `app/.dockerignore` | Excludes unnecessary files from build |
| `app/docker-compose.yml` | Local development environment |
| `app/build.sh` | Script to build and test locally |
| `app/validate_docker.py` | Script to confirm Stage A2 is complete |

**Result:**
- Image name: `slsa-demo-app:latest`
- Image ID: `5fbc20ebcfdd`
- Size: 207MB

---

### Stage A3 — GitHub Actions CI/CD Pipeline

**What it is:**
Automation that runs every time code is pushed to GitHub. It builds the Docker image, runs tests, and pushes the result to a registry — all without any manual steps.

**Why we built it:**
This turns GitHub into our **trusted builder**. Instead of "I built this on my laptop," we can now prove "GitHub Actions built this from commit `abc123` at exactly this timestamp in this environment."

This is critical for supply chain security. Anyone can claim anything about software they built locally. But if a neutral, auditable system (GitHub Actions) did the build, we have real proof.

**Before vs After:**

```
Before:
  Developer laptop → Docker build → "Trust me, it's fine" ❌

After:
  GitHub Actions → Docker build → SHA-256 digest → Build log → Proof ✅
```

**What the pipeline does automatically on every push:**

```
Step 1: Checkout code
Step 2: Set up Docker Buildx
Step 3: Log in to GitHub Container Registry (GHCR)
Step 4: Extract metadata (tags, labels)
Step 5: Build and push Docker image
Step 6: Extract SHA-256 digest
Step 7: Generate SBOM (Stage A4 addition)
Step 8: Upload SBOM artifacts
Step 9: Upload digest artifact
Step 10: Create build summary
```

**What is GHCR?**
GitHub Container Registry — GitHub's own Docker image hosting. When GitHub Actions pushes an image here, it gets a registry digest:

```
ghcr.io/adityamaitreya/slsaProj@sha256:abc123...
```

This is different from the local image ID. The registry digest is the official supply chain fingerprint.

**Files created:**

| File | Purpose |
|---|---|
| `.github/workflows/build.yml` | Main CI/CD pipeline — build, push, SBOM |
| `.github/workflows/test.yml` | Test pipeline — pytest, flake8 |
| `.gitignore` | Files Git should ignore |
| `validate_github_setup.sh` | Script to confirm Stage A3 is ready |

**Key concept:**
The `id-token: write` permission in the workflow is already set. This is what allows GitHub Actions to generate SLSA provenance in Stage A5. We planned ahead for it.

---

### Stage A4 — SBOM Generation

**What it is:**
Automatically generating a Software Bill of Materials — a complete list of every library, package, and dependency inside the Docker image.

**Why we built it:**
You cannot secure what you cannot see. The SBOM tells us exactly what is inside the software. If a new vulnerability is announced (like Log4Shell in 2021), we can instantly check if our software is affected.

**What is an SBOM?**
Think of it as the ingredient label on food packaging.

```
Tomato Sauce Label:           Software SBOM:
- Tomatoes                    - Flask 3.0.0
- Salt                        - Gunicorn 21.2.0
- Olive oil                   - Werkzeug 3.0.1
- Basil                       - Python 3.11.16
- ...                         - openssl 3.x
                              - ... (~150-200 packages total)
```

**Three SBOM formats generated:**

| Format | Standard | Best For |
|---|---|---|
| SPDX JSON | ISO/IEC 5962:2021 — Linux Foundation | Government, compliance |
| CycloneDX JSON | OWASP standard | Security teams, vulnerability tracking |
| Syft JSON | Anchore format | Detailed metadata, Grype scanning |

We generate all three because different tools and organizations prefer different formats.

**How Syft works:**

```
Syft scans the Docker image by its SHA-256 digest
         ↓
Finds every installed package (Python, system, etc.)
         ↓
Generates structured SBOM files
         ↓
Uploads them as build artifacts (kept for 90 days)
```

Important: Syft scans by **digest**, not by tag. This means it scans the exact, immutable image — not a "latest" that could change.

**Files created/modified:**

| File | Purpose |
|---|---|
| `.github/workflows/build.yml` | Updated — added 4 new SBOM steps |
| `app/generate_sbom.sh` | Script to generate SBOM locally |
| `SBOM_GUIDE.md` | Complete SBOM documentation |

**What you can do with the SBOM:**

```bash
# Count all packages
jq '.artifacts | length' sbom.syft.json

# Find a specific package
jq '.artifacts[] | select(.name == "flask")' sbom.syft.json

# List all licenses
cat sbom.spdx.json | jq '.packages[].licenseConcluded'

# Scan for vulnerabilities
grype sbom:sbom.syft.json
```

---

## Technical Terms Explained

### `cat << 'EOF'`

This is called a **heredoc** (here document). It is a bash trick for printing multiple lines of text cleanly.

```bash
# Without heredoc (messy):
echo "Line 1"
echo "Line 2"
echo "Line 3"

# With heredoc (clean):
cat << 'EOF'
Line 1
Line 2
Line 3
EOF
```

The word `EOF` is just a marker that says "start printing here" and "stop printing here." You could use any word — `END`, `STOP`, `DONE` — but `EOF` (End Of File) is the convention.

The single quotes around `'EOF'` mean: print everything literally, do not try to expand variables like `$HOME` or `$USER`.

We use it to print formatted summaries after each stage completes.

---

### Why Multiple `.sh` Script Files?

Each script has one clear job. This is the **single responsibility principle** — one tool, one purpose.

| Script | When You Run It | What It Does |
|---|---|---|
| `app/run_dev.sh` | During coding | Starts Flask locally |
| `app/build.sh` | Before pushing | Builds and tests Docker image locally |
| `app/generate_sbom.sh` | When you want to inspect | Generates SBOM from local image |
| `validate_github_setup.sh` | Before pushing to GitHub | Checks everything is ready |

**Local scripts vs GitHub Actions:**

```
Local scripts (.sh files):
- Run on YOUR laptop
- You run them manually
- For development and testing
- Fast feedback

GitHub Actions (.github/workflows/*.yml):
- Run on GitHub's servers automatically
- Triggered by git push
- Trusted, auditable builds
- Creates provenance
```

They serve different purposes. The local scripts help you work faster. GitHub Actions creates the official, trusted artifacts.

---

## Files Created So Far

```
projX/
├── .github/
│   └── workflows/
│       ├── build.yml          ← Main CI/CD (build, SBOM, push)
│       └── test.yml           ← Automated testing
├── .kiro/
│   ├── agents/
│   │   └── flask-docker-expert.md  ← Custom Kiro agent
│   ├── hooks/
│   │   └── auto-format.json   ← Auto-format Python on save
│   └── steering/
│       └── slsa-project-context.md ← Project context for Kiro
├── app/
│   ├── src/
│   │   └── app.py             ← Flask application
│   ├── tests/
│   │   └── test_app.py        ← 7 automated tests
│   ├── .dockerignore          ← Docker build exclusions
│   ├── .env.example           ← Config template
│   ├── build.sh               ← Local Docker build script
│   ├── docker-compose.yml     ← Local dev environment
│   ├── Dockerfile             ← Production multi-stage build
│   ├── Dockerfile.dev         ← Development build
│   ├── generate_sbom.sh       ← Local SBOM generation
│   ├── requirements.txt       ← Python dependencies
│   ├── run_dev.sh             ← Local Flask dev server
│   ├── validate_docker.py     ← Stage A2 validation
│   └── validate_structure.py  ← Stage A1 validation
├── .gitignore
├── DEVELOPMENT_ROADMAP.md     ← Full project roadmap with checkboxes
├── PROGRESS_SUMMARY.md        ← Progress tracker
├── projexplain.md             ← This file
├── SBOM_GUIDE.md              ← SBOM documentation
└── Software_Supply_Chain_Project_Roadmap.md  ← Original research plan
```

---

## Progress

| Stage | Name | Status |
|---|---|---|
| A1 | Sample Application | ✅ Complete |
| A2 | Dockerization | ✅ Complete |
| A3 | GitHub Actions CI/CD | ✅ Complete |
| A4 | SBOM Generation | ✅ Complete |
| A5 | SLSA Provenance | ✅ Complete |
| A6 | Cosign Signing | ✅ Complete |
| A7 | Common Storage Interface | ✅ Complete |
| A8 | PostgreSQL Backend | ✅ Complete |
| A9 | Verification Engine & API | ⏳ Next |
| A10 | React Dashboard | ⏳ Pending |
| A11 | Functional Testing Suite | ⏳ Pending |

**Overall: 8/20 stages complete (40%)**

---

### Stage A5 — SLSA Provenance Generation

**What it is:**
SLSA provenance is a cryptographic build receipt — a document that permanently records how, when, where, and from what source the software was built.

SLSA stands for **Supply-chain Levels for Software Artifacts** (pronounced "salsa"). It is an open framework created by Google and used by GitHub, npm, and many others.

**Why we built it:**
Anyone can claim "I built this from this source code." SLSA provenance makes that claim unforgeable. The provenance document is signed, attached to the image in the registry, and verifiable by anyone with the right tools.

**What it proves:**

| Question | Answer recorded in provenance |
|---|---|
| Who built it? | GitHub Actions (the exact workflow) |
| When? | Exact UTC timestamp |
| From which code? | Exact repository URL + commit SHA |
| How? | Exact workflow file and steps used |
| What was the result? | SHA-256 digest of the Docker image |

**How it works in our pipeline:**

The official SLSA generator runs as a **separate job** after the image is built and pushed. This separation is intentional — the generator needs the finished image digest as its input, and running it in a separate job keeps the trust boundary clean.

```yaml
# Job 2 in build.yml — runs after Job 1 finishes
slsa-provenance:
  uses: slsa-framework/slsa-github-generator/...@v2.0.0
  with:
    image: ghcr.io/adityamaitreya/slsaProj
    digest: sha256:abc123...   # output of Job 1
```

The generator:
1. Reads the GitHub Actions context (repo, commit, workflow, timestamp)
2. Builds a provenance document in SLSA v1.0 format
3. Signs it using GitHub's OIDC token
4. Pushes it as a signed OCI attestation attached to the image in GHCR

**What "attached to the image" means:**

The provenance is not a separate file you download. It is stored inside the same container registry, linked to the image by its digest. Think of it as a tamper-evident label fused to the jar — you cannot remove or replace it without breaking the seal.

**What the provenance document looks like (simplified):**

```json
{
  "buildType": "https://slsa.dev/container-based-build/v0.1",
  "builder": {
    "id": "https://github.com/slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.0.0"
  },
  "invocation": {
    "configSource": {
      "uri": "git+https://github.com/adityamaitreya/slsaProj@refs/heads/main",
      "digest": { "sha1": "abc123..." },
      "entryPoint": ".github/workflows/build.yml"
    }
  },
  "subject": [{
    "name": "ghcr.io/adityamaitreya/slsaproj",
    "digest": { "sha256": "5fbc20ebcfdd..." }
  }]
}
```

**Files changed:**
- `.github/workflows/build.yml` — added `slsa-provenance` job (Job 2)

---

### Stage A6 — Cosign Signing

**What it is:**
Cosign is a tool by Sigstore (part of the Linux Foundation) that digitally signs Docker images and attestations. Our pipeline uses **keyless signing**, which means we never create or manage a private key ourselves.

**Why keyless signing?**
Traditional signing requires you to:
1. Generate a private key
2. Keep it safe forever
3. Rotate it periodically
4. Handle key leaks

Keyless signing removes all of that. Instead, GitHub's OIDC (OpenID Connect) token proves identity in the moment. The certificate is short-lived (10 minutes). Nobody needs to manage keys.

**How keyless signing works step by step:**

```
GitHub Actions run starts
        ↓
GitHub issues a short-lived OIDC token
(proves: "this is GitHub Actions, repo X, workflow Y")
        ↓
Cosign exchanges that token with Sigstore's Fulcio CA
        ↓
Fulcio issues a short-lived signing certificate
(certificate contains: repo URL, workflow path, SHA)
        ↓
Cosign signs the image digest with that certificate
        ↓
Signature pushed to GHCR alongside the image
        ↓
Signature + certificate recorded in Rekor
(Rekor = public, append-only transparency log — like a blockchain)
        ↓
Certificate expires (10 min) — but the log entry is permanent
```

**What Rekor is:**
Rekor is a public transparency log run by Sigstore. Every signature we create is recorded there permanently. Anyone in the world can look up our signatures and verify they are authentic. It is similar to how Certificate Transparency logs work for TLS certificates.

**Three signing operations in our pipeline:**

| Operation | Command | What it does |
|---|---|---|
| Sign image | `cosign sign` | Signs the Docker image digest |
| Attest SBOM | `cosign attest --type cyclonedx` | Attaches the signed SBOM to the image |
| Attest provenance | Done by SLSA generator | Attaches signed SLSA provenance to image |

**How to verify our image (anyone can do this):**

```bash
cosign verify ghcr.io/adityamaitreya/slsaproj@sha256:... \
  --certificate-identity-regexp="https://github.com/adityamaitreya/slsaProj" \
  --certificate-oidc-issuer="https://token.actions.githubusercontent.com"
```

If this command succeeds, it proves:
- The image was signed inside a GitHub Actions run
- That run was for the `adityamaitreya/slsaProj` repository
- The signature is recorded in Rekor and has not been tampered with

If the image was modified after signing, this command will fail.

**Files changed:**
- `.github/workflows/build.yml` — added `cosign-installer` step, `cosign sign` step, `cosign attest` step in Job 1

---

## How A5 and A6 Fit Together

```
Docker Image Built
        ↓
SHA-256 Digest: sha256:abc123...
        ↓
        ├── cosign sign ──────────────────► Signature in GHCR + Rekor
        │                                   (proves image is authentic)
        │
        ├── cosign attest (SBOM) ─────────► SBOM attestation in GHCR
        │                                   (proves ingredients list is authentic)
        │
        └── SLSA generator ───────────────► Provenance attestation in GHCR
                                            (proves who/when/where/how it was built)
```

All three are attached to the same image digest. They cannot be separated or swapped without breaking the cryptographic links.

**The complete supply chain evidence package for each build:**

| Evidence | What it proves | Where stored |
|---|---|---|
| Docker image digest | Identity of the artifact | GHCR |
| Cosign signature | Image is authentic, not tampered | GHCR + Rekor |
| SBOM attestation | Dependency list is authentic | GHCR |
| SLSA provenance | Build origin and process | GHCR |

---

---

### Stage A7 — Common Storage Interface

**What it is:**
A Python abstraction layer that sits between the verification logic and the actual storage backends. It defines a contract — a set of methods that both PostgreSQL and Blockchain+IPFS must implement.

**Why we built it:**
This is the most important architectural piece of the research. Without it, the verification code would be tightly coupled to PostgreSQL, and swapping in the blockchain backend later would require rewriting everything.

With the interface in place:

```
Same verification code
        ↓
  ProvenanceStore (interface)
    /                \
PostgreSQLStore    BlockchainIPFSStore
    ↓                     ↓
System B (done)      System A (Phase B)
```

When we benchmark both systems in Phase C, any performance difference is **purely the storage backend**. The verification logic is identical. That is what makes the research scientifically valid.

**Four methods every backend must implement:**

| Method | What it does |
|---|---|
| `store(record)` | Save a provenance package |
| `retrieve(digest)` | Fetch by artifact SHA-256 fingerprint |
| `verify_integrity(digest)` | Recompute hash and compare — tamper detection |
| `health_check()` | Is the backend reachable? |

**Shared utility built into the interface:**

```python
# Compute a deterministic SHA-256 of the provenance JSON
ProvenanceStore.compute_provenance_hash(provenance_json)

# When you store a record, this hash is saved alongside it.
# When you verify, the hash is recomputed and compared.
# If anything changed in the database → hashes don't match → TAMPERED.
```

**Files created:**

| File | Purpose |
|---|---|
| `backend/models/provenance.py` | Data shapes: ProvenanceRecord, StoreResult, VerificationResult |
| `backend/storage/base.py` | Abstract interface + custom exceptions |
| `backend/storage/postgres_store.py` | PostgreSQL implementation |
| `backend/storage/blockchain_store.py` | Blockchain stub (Phase B) |
| `backend/tests/test_storage.py` | 17 tests — all passing |

**Key test — tamper detection:**
The most important test directly modifies the database row (bypassing the API) and then calls `verify_integrity()`. The system detects it and returns `TAMPERED`. This proves the hash-based tamper detection works even against direct database attacks.

---

### Stage A8 — PostgreSQL Backend

**What it is:**
A real PostgreSQL database running in Docker, with a FastAPI REST API on top of it. This is **System B (Centralized)** in the research comparison.

**Why we built it:**
The interface from A7 needs a real backend to prove it works. PostgreSQL is the simpler of the two backends, so we build it first. It also gives us a working system to benchmark against when the Blockchain+IPFS backend is ready.

**How it works:**

```
HTTP Request
     ↓
FastAPI (main.py)
     ↓
ProvenanceStore interface
     ↓
PostgreSQLStore
     ↓
PostgreSQL 14 (Docker container)
```

**The five endpoints:**

| Endpoint | What it does | Example response |
|---|---|---|
| `GET /` | API info and endpoint list | 200 JSON |
| `GET /health` | Is the database alive? | `{"database_connected": true}` |
| `POST /provenance` | Store a provenance record | `{"success": true, "backend": "postgresql"}` |
| `GET /provenance/{digest}` | Retrieve by artifact digest | Full ProvenanceRecord |
| `POST /verify/{digest}` | Check integrity | `{"status": "VALID"}` |

**What VALID, TAMPERED, NOT_FOUND mean:**

- `VALID` — record found, hash matches, nobody touched it
- `TAMPERED` — record found, but hash does not match — something changed
- `NOT_FOUND` — no record exists for this digest

**What we tested and confirmed:**

```
POST /provenance with wrong hash  → 500 rejected  ✅ (tamper at input)
POST /provenance with correct hash → 201 Created   ✅
GET  /provenance/{digest}          → 200 full record ✅
POST /verify/{digest}              → VALID          ✅
GET  /provenance/sha256:doesnotexist → 404          ✅
```

**Files created:**

| File | Purpose |
|---|---|
| `backend/docker-compose.yml` | PostgreSQL 14 container setup |
| `backend/.env` | Database connection string |
| `backend/main.py` | FastAPI application with all 5 endpoints |

**Important detail — CORS middleware:**
The FastAPI app has CORS (Cross-Origin Resource Sharing) enabled for `localhost:3000` and `localhost:5173`. This means the React dashboard (Stage A10) can call this API from the browser without being blocked.

---

## What Comes Next

### Stage A9 — Verification Engine & API

This builds the core verification logic that runs ALL checks on an artifact:

```
Artifact digest
     ↓
1. Fetch provenance from store
2. Verify Cosign signature
3. Validate SLSA provenance format
4. Check source repository matches
5. Check builder identity matches
6. Check provenance hash integrity
     ↓
VALID / TAMPERED / INVALID
```

This is different from the simple hash check we have now. Stage A9 adds cryptographic signature verification and full SLSA provenance validation.
- **Verification API:** Same code, same logic, pluggable backend

---

*Last updated after Stage A8 completion*
*Repository: https://github.com/adityamaitreya/slsaProj*